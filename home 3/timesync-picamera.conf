# AP モードでは NTP 不可。テザリング時は自動同期、再起動後は fake-hwclock で復元。
[Time]
NTP=0.pool.ntp.org 1.pool.ntp.org time.google.com
FallbackNTP=pool.ntp.org
PollIntervalMinSec=32
PollIntervalMaxSec=2048
ConnectionRetrySec=30
